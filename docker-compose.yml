services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
    - db

  db:
    image: postgres:16
    environment:
      POSTGRES_DB: career_journey
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    PORTS:
      - "5432:5432"